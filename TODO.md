1. Make sync button show that it is active.
2. Navigation element to view next/previous pull requests from the list (from the pull request page). A keystroke on that page would also be good.
3. Ticking an item checkbox done in the code review section should load the next file diff (quailty of life improvement).
4. Create worktree dropdown needs better design. Errors from this operstion should be shown as toast notifications as well (ones that need to be acknoledged and dismissed).
5. Create worktree user feedback needs to improve - its not feeling very responsive and unclear to the user if they should click again.
6. Validate if worktree exists already for a pr. Update that status when the page loads, and when the user tries to create a new worktree.
7. Navbar bookmark count was 1, but the bookmarks page showed 0
